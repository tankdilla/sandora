%h1 Listing quantity_types

%table
  %tr
    %th Description
    %th
    %th
    %th

  - @quantity_types.each do |quantity_type|
    %tr
      %td= quantity_type.description
      %td= link_to 'Show', quantity_type
      %td= link_to 'Edit', edit_quantity_type_path(quantity_type)
      %td= link_to 'Destroy', quantity_type, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Quantity type', new_quantity_type_path
