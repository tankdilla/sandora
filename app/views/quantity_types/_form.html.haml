= form_for @quantity_type do |f|
  -if @quantity_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@quantity_type.errors.count, "error")} prohibited this quantity_type from being saved:"
      %ul
        - @quantity_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
